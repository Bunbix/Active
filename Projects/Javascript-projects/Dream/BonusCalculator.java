class BonusCalculator {
    public static void main(String[] args) {
        // Declare AND assign in one step
        double salary = 50000;
        double bonus = 0.02 * salary;
        String bonusMessage = "Bonus = $" + bonus;
        
        System.out.println(bonusMessage);
    }
}